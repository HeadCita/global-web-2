doctype html
html(lang= "es")
    head
        meta(charset="UTF-8")
        meta(name="viewport" content="width=device-width,initial-scale=1")
        meta(name="description" content=description)
        meta(name="keywords" content="escuela de manejo, obtencion de brevetes, examen de manejo, escuela de conductores, aprender a manejar")
        meta(name="author" content="HeadCita")
        meta(name="owner" content="Multiservicios Global Car SAC")
        title= title
    body
        include partials/header